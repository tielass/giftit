<div id="message-<%= message.id %>">
  <small>
    <strong><%= User.where(id: message.user_id)[0].username.capitalize %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
